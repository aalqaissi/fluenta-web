node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example
!.env.production
npm-debug.log*
.vite

# tsc -b build artifacts (regenerated on build)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# generated knowledge graph (regenerable via /graphify)
graphify-out/

# large source media / recordings — keep out of git (GitHub 100MB/file limit)
artifact/

# backend (Spring Boot / Maven) build output + local SQLite database file
backend/target/
backend/data/
backend/.mvn/wrapper/maven-wrapper.jar
# bundled frontend build (copied in by package.cmd at packaging time)
backend/src/main/resources/static/
# single-binary deploy bundle (produced by package.cmd)
dist-app/
